#!/usr/bin/env bash

case "$1" in
  "-p")
    task build --output prefixed
    ;;
  "-v")
    task build --output prefixed --verbose
    ;;
  "")
    task build --output group --output-group-error-only
    ;;
  *)
    task build "$@"
    ;;
esac
